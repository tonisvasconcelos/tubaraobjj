# Frontend (Vite). Copy to .env and fill.
# API do backend (Node). Em produção use a URL do Render/Railway.
VITE_API_URL=http://localhost:4000
# Base URL do site. Use / para Vercel/Netlify na raiz; use /tubaraobjj/ para GitHub Pages.
# VITE_BASE_URL=/
